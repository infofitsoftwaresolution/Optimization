name: CI/CD Pipeline - Deploy to EC2

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run linting and syntax checks
      run: |
        echo "🔍 Running syntax checks..."
        python -m py_compile src/*.py config.py || true
        python -m py_compile scripts/*.py || true
      continue-on-error: true

    - name: Verify configuration files
      run: |
        echo "✅ Verifying configuration files exist..."
        test -f requirements.txt && echo "✓ requirements.txt found"
        test -f config.py && echo "✓ config.py found"
        test -f src/dashboard.py && echo "✓ dashboard.py found"

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.EC2_SSH_PORT || 22 }}
        script: |
          set -e  # Exit on error
          
          echo "🚀 Starting automated deployment..."
          echo "📅 Deployment time: $(date)"
          echo "🔗 Commit: ${{ github.sha }}"
          echo "👤 Committed by: ${{ github.actor }}"
          
          # Navigate to project directory
          # Detect user from EC2_USER secret or default to ubuntu
          EC2_USERNAME="${{ secrets.EC2_USER }}"
          if [ "$EC2_USERNAME" = "ec2-user" ]; then
            PROJECT_DIR="/home/ec2-user/Optimization"
          else
            PROJECT_DIR="/home/ubuntu/Optimization"
          fi
          
          if [ ! -d "$PROJECT_DIR" ]; then
            echo "⚠️  Project directory not found. Creating it..."
            mkdir -p "$PROJECT_DIR"
            cd "$PROJECT_DIR"
            git clone https://github.com/infofitsoftwaresolution/Optimization.git .
          else
            cd "$PROJECT_DIR"
          fi
          
          echo "📥 Pulling latest code from main branch..."
          git fetch origin
          git reset --hard origin/main
          git clean -fd  # Remove untracked files
          
          echo "📦 Installing/upgrading Python dependencies..."
          python3 -m pip install --upgrade pip setuptools wheel
          pip3 install -r requirements.txt --upgrade --no-cache-dir
          
          echo "🧹 Clearing Streamlit cache..."
          python3 -m streamlit cache clear 2>/dev/null || true
          rm -rf .streamlit/cache 2>/dev/null || true
          
          echo "🔍 Running diagnostic test..."
          if [ -f "scripts/test_streamlit.sh" ]; then
            chmod +x scripts/test_streamlit.sh
            bash scripts/test_streamlit.sh || true
          fi
          
          echo "🔄 Restarting application..."
          
          # Kill existing Streamlit processes
          echo "Stopping existing Streamlit processes..."
          pkill -f "streamlit run src/dashboard.py" 2>/dev/null || true
          sleep 3
          
          # Ensure PATH includes user's local bin
          export PATH="$HOME/.local/bin:$PATH"
          echo "PATH set to: $PATH"
          
          # Make startup script executable if it exists
          if [ -f "scripts/start_streamlit.sh" ]; then
            chmod +x scripts/start_streamlit.sh
            echo "✅ Using startup script: scripts/start_streamlit.sh"
            echo "Starting Streamlit in background..."
            cd "$PROJECT_DIR"
            nohup bash scripts/start_streamlit.sh "$PROJECT_DIR" > dashboard.log 2>&1 &
            STREAMLIT_PID=$!
            echo "Started with PID: $STREAMLIT_PID"
            sleep 2
            # Verify the process is still running
            if ! ps -p $STREAMLIT_PID > /dev/null 2>&1; then
              echo "⚠️  Process $STREAMLIT_PID exited immediately, checking logs..."
              if [ -f dashboard.log ]; then
                cat dashboard.log
              fi
            fi
          else
            # Fallback: Use python3 -m streamlit directly
            echo "⚠️  Startup script not found, using direct command"
            echo "Starting Streamlit with: python3 -m streamlit"
            cd "$PROJECT_DIR"
            nohup bash -c "export PATH=\"\$HOME/.local/bin:\$PATH\" && cd $PROJECT_DIR && python3 -m streamlit run src/dashboard.py --server.port 8501 --server.address 0.0.0.0 --server.headless true --server.enableCORS false --server.enableXsrfProtection false" > dashboard.log 2>&1 &
            STREAMLIT_PID=$!
            echo "Started with PID: $STREAMLIT_PID"
          fi
          
          # Wait longer for Streamlit to start
          echo "Waiting 15 seconds for Streamlit to start..."
          sleep 15
          
          # Check if log file was created
          if [ ! -f "dashboard.log" ]; then
            echo "⚠️  Warning: dashboard.log not created yet"
            echo "This means Streamlit failed to start immediately"
          else
            echo "✅ Log file created, showing last 30 lines:"
            tail -30 dashboard.log || true
            echo ""
            echo "Checking for errors in log:"
            grep -i "error\|exception\|traceback\|failed\|cannot" dashboard.log | tail -20 || echo "No obvious errors found"
          fi
          
          # Check if process is still running
          if ps -p $STREAMLIT_PID > /dev/null 2>&1; then
            echo "✅ Streamlit process (PID $STREAMLIT_PID) is running!"
          elif pgrep -f "streamlit run src/dashboard.py" > /dev/null; then
            echo "✅ Streamlit application is running (found via pgrep)!"
          else
            echo "❌ Failed to start Streamlit application"
            echo "📋 Process check:"
            ps aux | grep streamlit | grep -v grep || echo "No streamlit process found"
            echo ""
            echo "📋 Last 50 lines of log:"
            if [ -f dashboard.log ]; then
              tail -50 dashboard.log
            else
              echo "Log file not created - Streamlit failed to start immediately"
            fi
            echo ""
            echo "📋 Checking for Python errors:"
            if [ -f dashboard.log ]; then
              grep -i "error\|exception\|traceback\|failed" dashboard.log | tail -20 || echo "No obvious errors in log"
            fi
            exit 1
          fi
          
          echo "📊 Application status:"
          ps aux | grep streamlit | grep -v grep || echo "Process check complete"
          
          echo "🌐 Checking if application is responding..."
          sleep 2
          curl -f http://localhost:8501/_stcore/health > /dev/null 2>&1 && \
            echo "✅ Application is healthy and responding!" || \
            echo "⚠️  Application may still be starting up..."
          
          echo ""
          echo "✅ Deployment completed successfully!"
          echo "📊 Dashboard available at: http://${{ secrets.EC2_HOST }}:8501"
          echo "📅 Deployment completed at: $(date)"

